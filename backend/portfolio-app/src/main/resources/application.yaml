spring:
  application:
    name: portfolio-app

  datasource:
    url: #need url here
    driver-class-name: org.postgresql.Driver

  flyway:
    locations: classpath:db/migration 
    baseline-on-migrate: true

  # jpa:
  #   hibernate:
  #     ddl-auto: validate
  #   database-platform: org.hibernate.dialect.PostgreSQLDialect
  # Not sure if we need this, something to do with Java and SQL



    #Ask copilot to see if i did application, application.local correct and then continue from steps